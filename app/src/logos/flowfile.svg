<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <linearGradient id="ffGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#6366f1"/>
      <stop offset="100%" stop-color="#06b6d4"/>
    </linearGradient>
  </defs>
  <!-- Background circle -->
  <circle cx="50" cy="50" r="46" fill="url(#ffGrad)"/>
  <!-- Flow pipe — left segment -->
  <rect x="12" y="43" width="30" height="14" rx="7" fill="white" opacity="0.9"/>
  <!-- Flow pipe — right segment -->
  <rect x="58" y="43" width="30" height="14" rx="7" fill="white" opacity="0.9"/>
  <!-- Arrow head (right) -->
  <polygon points="72,36 88,50 72,64" fill="white"/>
  <!-- Center node dot -->
  <circle cx="50" cy="50" r="10" fill="white"/>
  <circle cx="50" cy="50" r="6" fill="url(#ffGrad)"/>
</svg>
